<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{.name}}</title>
</head>
<body>
    <div style="display: flex; justify-content: center;">
        <div>
            <a href="{{.PrevUrl}}">Previous</a>
            <a href="{{.NextUrl}}">Next</a>
        </div>
        <a href="{{.NextUrl}}"><img src="{{.ResourceUrl}}" alt="{{.Name}}" style="max-width: 80%; max-height: 80%;"></a>
    </div>

    <div class="image-grid">
        {{range .Others}}
            <a href="{{.Url}}"><img style="max-width: 25%; height: auto;" src="{{.ResourceUrl}}" alt="{{.Name}}"></a>
        {{end}}
    </div>
</body>
</html>
